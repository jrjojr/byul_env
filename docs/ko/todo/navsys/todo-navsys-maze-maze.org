#+TITLE: BYUL Navsys maze.h 정밀 리팩터링 TODO
#+TODO: TODO DOING BLOCKED | DONE CANCELLED FAILED
#+TODO_RULES: ../../../../external/rules/projects/byul/todo-rules.org
#+PUBLIC_API_RULES: ../../../../external/rules/projects/byul/public-api-rules.org
#+HEADER_DECLARATION_RULES: ../../../../external/rules/projects/byul/header-declaration-rules.org
#+HEADER_COMMENT_RULES: ../../../../external/rules/projects/byul/header-comment-rules.org
#+HEADER_AUTHORING_RULES: ../../../../external/rules/projects/byul/header-authoring-rules.org
#+BUILD_RULES: ../../../../external/rules/projects/byul/build-rules.org
#+TEST_RULES: ../../../../external/rules/projects/byul/test-rules.org
#+TODO_PARENT: todo-리팩토링-navsys-headers.org
#+LANGUAGE: ko
#+OPTIONS: toc:3 num:nil

* 대상과 결론

- 대상: =byul/navsys/maze/maze.h=, =maze.cpp=, maze CMake inventory와 native consumer.
- 역할: 11개 generator를 선택하는 public C aggregate/dispatcher이다.
- 판정: enum ABI와 기존 =maze_make= symbol은 보존하되, silent fallback을 제거할 수 있는
  status/out-parameter/options 기반 API를 먼저 추가한다. 기존 함수의 signature를 직접
  바꾸지 않는다.
- =maze_type_t=의 현재 값 0..10은 binary ABI fixture로 동결한다. 새 enum member를 중간에
  삽입하지 않는다.
- 공통 status/options/callback 계약은
  [[file:todo-navsys-industry-api-baseline.org][Navsys 업계 API 기준]]이 우선한다. 아래
  =navsys_status_t=와 symbol prefix는 공통 결정 전의 구체 검토안이다.

* 책임 경계와 실제 검증 파일

- 이 문서와 =maze.cpp=는 enum-to-generator selection, 공통 options 검증·routing,
  unsupported/fallback 판정과 11개 generator capability의 합류를 소유한다.
- 개별 algorithm state machine, RNG 소비·seed/replay와 generation loop 구현은 각 generator
  전용 TODO가 소유한다. dispatcher는 승인된 context를 전달할 뿐 그 구현을 복제하지 않는다.
- =maze_core.cpp=의 resource/extent/enumeration/overlay primitive는
  [[file:todo-navsys-maze-core.org][maze core TODO]]가 소유하며 dispatcher가 재구현하지 않는다.
- 실제 maze native 검증 파일은
  =byul/navsys/maze/tests/test_module_maze.cpp=,
  =byul/navsys/maze/tests/test_module_maze_discrete.cpp=,
  =byul/navsys/maze/tests/test_module_maze_room.cpp=다. 이 문서는 세 파일의 dispatcher
  selection/capability 통합 case를 합류 검증한다.

* 현행 선언과 구현 지도

| 항목 | 현재 사실 | 결함/위험 |
|------+-----------+-----------|
| include | =maze_core.h= 하나 | aggregate가 core에 의존하는 방향은 맞지만 표준 license/file Doxygen/BYUL guard가 없다 |
| dispatcher | =maze_make(..., maze_type_t)= | =default=가 Kruskal을 실행해 invalid/unsupported를 숨긴다 |
| randomness | 선택된 함수 내부에서 =time=, =random_device=, =rand=를 제각각 사용 | seed를 지정·회수·재현할 수 없다 |
| error | 실패를 =NULL= 하나로 표시 | invalid dimension, unsupported type, OOM, cancelled를 구분하지 못한다 |
| termination | synchronous 단일 호출 | step/time/cancel budget가 없어 random-walk/large input을 제어할 수 없다 |
| wrapper | wrapper manifest 미등록 | public export인데 Python 소비 여부와 제외 사유가 없다 |
| build | maze static 및 root shared inventory에 포함, SDK install 대상 | module static 통과만으로 root export를 보증할 수 없다 |
| test | 11개 enum 각각 non-NULL/print 중심 | invalid enum이 Kruskal로 바뀌는 현행 오류를 assert하지 않는다 |

* Public symbol inventory와 처분

| kind | 현재 symbol | 현재 ABI | 처분 |
|------+-------------+----------+------|
| guard macro | =MAZE_H= | source macro | source macro: ABI 1.x: keep -> ABI-major: internalize; collision-safe guard companion: ABI 1.x: add |
| enum type | =maze_type_t= | public, compiler enum representation | legacy type: ABI 1.x: keep -> ABI-major: abi-major; prefixed alias companion: ABI 1.x: add |
| enumerator | =MAZE_TYPE_RECURSIVE= | 0 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_PRIM= | 1 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_BINARY= | 2 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_ELLER= | 3 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_ALDOUS_BRODER= | 4 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_WILSON= | 5 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_HUNT_AND_KILL= | 6 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_SIDEWINDER= | 7 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_RECURSIVE_DIVISION= | 8 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_KRUSKAL= | 9 | ABI 1.x: keep -> ABI-major: keep |
| enumerator | =MAZE_TYPE_ROOM_BLEND= | 10 | ABI 1.x: keep -> ABI-major: keep |
| function | =maze_make= | owned =maze_t*= 또는 NULL | legacy: ABI 1.x: deprecate-forward -> ABI-major: internalize; canonical companion: ABI 1.x: add |

* 업계 관행과 비교 기준

- [[https://www.boost.org/doc/libs/1_48_0/libs/graph/doc/random_spanning_tree.html][Boost.Graph random_spanning_tree]]는 graph, caller 제공 RNG, root와 output map을
  분리하고 연결성 precondition 및 stuck failure를 명시한다. BYUL dispatcher는 RNG와 실패
  채널이 모두 숨겨져 있다.
- [[https://recastnav.com/classdtNavMeshQuery][Recast Detour query API]]는 status, caller buffer/capacity, partial result와 sliced
  progress를 구분한다. 긴 maze generation에도 status/cancel/progress 계약을 적용한다.
- [[https://www.open-std.org/jtc1/sc22/wg21/docs/papers/2016/p0205r0.html][WG21 P0205R0]]는 deterministic seed sequence와 nondeterministic seed source를 별도
  개념으로 다룬다. =time(NULL)=을 각 구현에 숨기는 현재 방식은 재현·병렬성 관행과 맞지 않는다.

* 목표 API 검토안

다음은 구현 전에 ABI review로 확정할 구체 후보이다. 공통 type은 =maze_core.h= 계획과
동시에 확정한다.

#+begin_src c
typedef enum byul_maze_algorithm {
    BYUL_MAZE_ALGORITHM_RECURSIVE_BACKTRACKER = 0,
    BYUL_MAZE_ALGORITHM_RANDOMIZED_PRIM = 1,
    BYUL_MAZE_ALGORITHM_BINARY_TREE = 2,
    BYUL_MAZE_ALGORITHM_ELLER = 3,
    BYUL_MAZE_ALGORITHM_ALDOUS_BRODER = 4,
    BYUL_MAZE_ALGORITHM_WILSON = 5,
    BYUL_MAZE_ALGORITHM_HUNT_AND_KILL = 6,
    BYUL_MAZE_ALGORITHM_SIDEWINDER = 7,
    BYUL_MAZE_ALGORITHM_RECURSIVE_DIVISION = 8,
    BYUL_MAZE_ALGORITHM_RANDOMIZED_KRUSKAL = 9,
    BYUL_MAZE_ALGORITHM_ROOM_BLEND = 10
} byul_maze_algorithm_t;

BYUL_API navsys_status_t byul_maze_algorithm_is_supported(
    byul_maze_algorithm_t algorithm, bool* out_supported);

BYUL_API navsys_status_t byul_maze_generate(
    byul_maze_algorithm_t algorithm,
    int32_t origin_x, int32_t origin_y,
    uint32_t width, uint32_t height,
    const byul_maze_generate_options_t* options,
    maze_t** out_maze);
#+end_src

Options 후보에는 =struct_size=, =abi_version=, =uint64_t seed=, seed 사용 여부,
=uint64_t max_steps=, cancel callback와 =void* userdata=를 둔다. 성공 때만
=*out_maze=가 caller-owned가 되며 실패 때는 NULL이어야 한다. callback은 호출 thread,
retention 없음, reentrancy 금지/허용을 명시한다.

* 호환성과 ABI-major 경로

1. 현 enum numeric fixture와 export/signature를 먼저 동결한다.
2. additive =byul_maze_generate=와 support query를 구현한다.
3. =maze_make=는 새 함수로 forwarding하되 기존 nondeterministic seed 정책을 유지하고
   invalid enum은 과거 fallback 호환 여부를 release policy로 결정한다. 동작 변경이
   허용되지 않으면 legacy adapter만 Kruskal fallback을 유지하고 명확히 deprecate한다.
4. wrapper는 새 status API를 기본으로 노출하고 legacy는 compatibility namespace에 둔다.
5. enum storage와 =maze_t= opaque 전환은 ABI-major에서만 한다.

* DONE 1단계: ABI·dispatcher 기준선 동결

** 실행

- header enum 값, =sizeof(maze_type_t)=, =maze_make= calling convention/export를 fixture로 만든다.
- 11개 switch branch와 invalid values =-1, 11, =INT_MAX=의 현재 결과를 기록한다.
- static/root/install/wrapper/umbrella 소비 경로와 모든 direct include를 수집한다.

** 완료 조건

- enum과 symbol의 platform별 baseline이 있고 silent Kruskal fallback이 회귀 test로 재현된다.
- 각 algorithm header의 child TODO와 dispatcher enum이 1:1 대응한다.

** 검증

- MSVC/MinGW/Linux에서 enum value/size와 root shared export snapshot을 비교한다.
- C17/C++17 standalone include consumer와 legacy =maze_make= binary fixture를 실행한다.

** 진행 근거

- =maze_type_t= 값 0..10, 4-byte 크기와 =maze_make= signature를 compile-time fixture로
  고정했다. 11개 switch branch와 default Kruskal mapping을 source inventory에 기록했다.
- invalid enum =-1=, =11=, =INT_MAX=가 3x3 explicit Kruskal과 semantic/hash가 같은
  owned maze를 반환하는 현행 silent fallback을 회귀 test로 재현했다.
- =maze.h= direct include C17/C++17 SDK consumer를 추가했다. MSVC Release 및 Virtual Yard
  GCC 13에서 build/run했고 Clang 18 module/root build도 통과했다.
- MSVC Release =test_maze=와 Virtual Yard GCC 13/Clang 18 Debug ASan/LSan =test_maze=는
  각각 37/37, 248 assertions를 통과했다. MSVC root =byul.dll=의 =maze_make= export도 확인했다.
- =maze-dispatcher-stage1-baseline.json=에 소비 경로, wrapper 미등록 상태와 platform 결과를
  기록했다. Virtual Yard MinGW64 GCC/G++ 13으로 root Windows DLL, C/C++17 SDK fixture를
  compile/link하고 =maze_make= PE export를 확인해 다음 단계 gate를 허용했다.

* DONE 2단계: 공통 generation 계약 확정

** 실행

- 좌표는 cell index, bounds는 =[origin, origin+extent)=, width/height는 odd >=3인지
  algorithm별 capability table로 정의한다.
- seed, RNG algorithm/version, max steps, cancellation, OOM, output ownership을 확정한다.
- perfect/tree, connected, border-wall, bias, uniform-spanning-tree 여부를 algorithm별로
  machine-readable capability에 기록한다.

** 완료 조건

- 같은 seed/algorithm/options의 재현 범위가 동일 build/platform 또는 cross-platform 중
  하나로 명확히 선택된다.
- unsupported dimension/topology가 fallback 없이 status로 관찰된다.

** 검증

- capability matrix와 11개 구현의 declared contract를 자동 비교한다.
- invalid dimension, invalid enum, max-step, cancel-before-start/cancel-mid-run fixture를 설계 검토한다.

** 마감 근거

- =maze-dispatcher-stage2-contract.json=에 half-open 좌표계, versioned options,
  PCG32 XSH-RR 상태 전이·초기화·bounded sampling, step/cancel/OOM/status와 실패 원자성을
  구현 가능한 계약으로 고정했다. 재현 범위는 같은 algorithm behavior version, seed,
  options에 대한 cross-platform/cross-toolchain으로 선택했다.
- enum 0..10과 실제 dispatcher 함수 11개를 1:1로 연결하고 최소 크기·parity,
  connected/acyclic/perfect/border/UST/bias, budget unit과 child support gate를
  machine-readable capability matrix에 기록했다. Recursive Division의 미검증 topology는
  추측하지 않고 null로 남겼다.
- canonical API는 child gate 전에는 아무 algorithm도 advertise하지 않으며 invalid enum과
  unsupported dimension을 allocation/RNG 소비 전 =NAVSYS_STATUS_UNSUPPORTED=로 끝내도록
  확정했다. 현재 Kruskal default는 ABI 보존 대상인 legacy adapter로만 분리했다.
- =test_maze_dispatcher_contract.py= 2 tests가 enum/source/matrix/RNG/fail-closed 불변식을
  통과했다. wrapper generated check, Navsys ABI inventory 57 headers/644 symbols, 중앙 규칙
  validator 60 files, =git diff --check=도 모두 exit 0이다.

* DONE 3단계: additive API와 legacy adapter 구현

** 실행

- support query, options 검증, status/out API를 구현하고 switch의 default를 unsupported로 처리한다.
- 각 generator에 동일 RNG/cancel/budget context를 전달한다.
- legacy =maze_make= forwarding과 deprecation metadata를 추가한다.

** 진행 근거

- 공통 PCG32/cancel/step-budget context를 구현하고 Recursive Backtracker, Randomized Prim,
  Binary Tree, Eller, Aldous-Broder, Wilson, Hunt-and-Kill, Sidewinder, Recursive Division,
  Randomized Kruskal, Room Blend 11개 생성기를 status/out 내부 경로로 전환했다.
  Sidewinder 첫 행은 canonical
  forced run으로, Recursive Division은 열린 내부 분할로 수정했고 Room Blend modulo-zero와
  이중 RNG 결함을 제거했다.
  legacy entry point는 비결정 seed adapter로 유지하며 checked dispatcher route도 연결했다.
- 동일 seed와 non-zero origin 재현, =max_steps=1= 실패 원자성 fixture를 포함한 MSVC Release
  Maze 테스트 41개/372 assertion이 통과했다. wrapper 정책/계약 6개, generated check,
  Navsys ABI inventory 57 headers/646 symbols, 중앙 규칙 60 files도 통과했다.
- canonical support query는 각 child topology gate가 끝날 때까지 계속 fail-closed다.
- 설치 트리의 dispatcher header, current aggregate SDK, ABI-1 compatibility consumer를
  C/C++ 각각 새 root DLL에 link/run하여 6개 실행 파일이 모두 exit 0임을 확인했다.

** 완료 조건

- 신규 API가 invalid/OOM/cancel/unsupported를 구분하고 실패 시 output을 남기지 않는다.
- legacy symbol과 enum 값은 그대로 export된다.

** 검증

- old/new consumer를 같은 root shared library에 link/run한다.
- wrapper audit/check/native load와 export inventory를 검사한다.

* DONE 4단계: 통계·위상·종료 test gate

** 실행

- 각 generator TODO가 자기 algorithm의 fixed-seed golden hash, connectivity, edge count,
  cycle, border invariant를 소유하게 하고 dispatcher는 capability 선언과 결과를 합류한다.
- Aldous-Broder/Wilson TODO만 uniformity claim과 작은 graph 분포 검증을 소유하며
  dispatcher는 해당 capability가 검증 결과와 일치하는지만 확인한다.
- max step과 cancellation이 bounded latency로 끝나며 partial output을 반환하지 않는지 검사한다.

** 진행 근거

- 11개 생성기 × 6개 고정 seed corpus에서 non-zero origin 9×9 출력을 분석해 경계 차단,
  논리 셀 개방, BFS 연결성을 검증했다. perfect 계열 10개는 edge count = V-1도 통과했고
  Room Blend는 connected cyclic 허용 조건을 통과했다.
- seed 0의 알고리즘별 golden =maze_hash= 11개를 고정했으며, 모든 생성기의 mid-run
  cancellation과 callback exception이 partial output 없이 각각 CANCELLED/CALLBACK_FAILED로
  끝나는지 검증했다.
- Aldous-Broder와 Wilson은 2×2 논리 그래프의 spanning tree 4종을 각각 4,096개 seed로
  분류했다. 기대 bucket 1,024에 대해 사전 고정한 850..1,200 허용 범위를 모두 통과했다.
- 계약 default multiplier로 11개 생성기 × 64 seeds가 모두 step limit 안에 종료했다.
- Linux GCC ASan/LSan 전체 Maze suite는 46 tests/59,756 assertions로 sanitizer와 leak
  오류 없이 통과했다.
- correctness gate 이후 11개 algorithm support를 모두 개방했다. public checked dispatcher,
  Python wrapper, 설치 SDK C/C++ header consumer가 모든 enum을 실제 생성했고 MSVC Release
  Maze suite는 46 tests/59,756 assertions, root =test_byul=은 533 tests/126,449 assertions로
  통과했다. current/ABI-1 SDK consumer C/C++ 6개도 모두 exit 0이다.

** 완료 조건

- print/non-NULL test가 논리 invariant test로 대체되고 flaky 허용 범위가 근거를 가진다.
- algorithm claim과 실제 topology가 불일치하면 dispatcher에서 supported로 보고하지 않는다.

** 검증

- maze static test, root =test_byul=, ASan/LSan과 반복 seed corpus를 실행한다.
- clean installed SDK C consumer에서 모든 enum과 status path를 실행한다.

* DONE 5단계: Header 규칙과 release gate

** 실행

- 표준 license, =@file/@brief=, =BYUL_= guard, direct include와 =@byul.*= metadata를 적용한다.
- enum support, ownership, thread/cancel/RNG 계약과 deprecated timeline을 release-note 후보로 남긴다.

** 완료 조건

- header audit 0 error이며 declaration/definition/wrapper signature가 일치한다.
- root shared가 module과 동일한 maze inventory를 직접 compile/export한다.

** 검증

- header audit/check, C/C++ standalone compile, ABI/export diff와 SDK consumer를 실행한다.
- module static =BYUL_STATIC=과 root shared =BYUL_EXPORTS=에서 dispatcher와 11개 generator inventory를 각각 검증한다.
- Windows/Linux/macOS 미실행 matrix는 완료로 숨기지 않고 기록한다.

** 완료 증거

- Maze 공개 header 13개에 표준 license, =@file/@brief=, =BYUL_= guard와
  ownership/thread/cancel/RNG/deprecation metadata를 적용했다. 전체 저장소의 기존 header
  debt와 분리한 Maze scoped audit는 0 error이고 wrapper generated check도 통과했다.
- 설치 header 13개를 MSVC 19.44와 WSL GCC 13.3에서 C/C++ 각각 standalone compile해
  26/26 조합이 두 toolchain 모두 통과했다.
- MSVC Release module static =maze.lib=와 root =byul.dll=을 다시 빌드했다. module
  =test_maze=는 46 tests/59,756 assertions가 통과했고 root DLL과 Linux =libbyul.so=에서
  checked dispatcher, Maze core와 11개 legacy generator symbol을 직접 확인했다.
- clean installed SDK의 dispatcher header, aggregate, ABI-1 compatibility C/C++ consumer
  6개를 Windows와 Linux에서 모두 실행했다. Linux 실행은 기존 ASan Debug root shared에
  맞춰 =libasan.so.8=을 preload하고 leak detection을 유지했다.
- MSVC ABI probe는 79 complete types/393 fields, 30 enums/171 enumerators로 통과했고
  release snapshot은 1,201 exports, 116 install files, 95 headers, 19 tests로 고정했다.
  Navsys inventory는 57 headers/646 symbols, header-role manifest는 149 headers이며 Maze
  hash mismatch가 없다. 중앙 규칙 validator는 60 files, 0 error/0 warning이다.
- 플랫폼 matrix는 Windows MSVC와 Linux WSL GCC를 실행했다. 현재 환경에 macOS runner가
  없어 macOS compile/runtime은 미실행이며, 이를 전체 3-platform matrix 완료로 간주하지
  않는다. macOS release CI 또는 실제 runner에서 동일 SDK consumer gate를 후속 실행한다.
