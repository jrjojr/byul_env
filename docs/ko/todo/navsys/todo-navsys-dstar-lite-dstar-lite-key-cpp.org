#+TITLE: BYUL Navsys dstar_lite_key.hpp 정밀 리팩토링 TODO
#+TODO: TODO DOING BLOCKED | DONE CANCELLED FAILED
#+TODO_STATUS: DONE
#+RELEASE_NOTE_REQUIRED: yes
#+RELEASE_CHANGE_KIND: compatibility
#+RELEASE_LEVEL: major
#+RELEASE_TARGET: developer
#+RELEASE_TITLE: D* Lite legacy C++ key helper 제거
#+RELEASE_GOAL: 설치 및 소스 트리의 key ordering helper를 private 경계로 통일한다.
#+RELEASE_BREAKING: yes
#+HELP_UPDATE_REQUIRED: no
#+TODO_RULES: ../../../../external/rules/projects/byul/todo-rules.org
#+PUBLIC_API_RULES: ../../../../external/rules/projects/byul/public-api-rules.org
#+HEADER_DECLARATION_RULES: ../../../../external/rules/projects/byul/header-declaration-rules.org
#+HEADER_COMMENT_RULES: ../../../../external/rules/projects/byul/header-comment-rules.org
#+HEADER_AUTHORING_RULES: ../../../../external/rules/projects/byul/header-authoring-rules.org
#+BUILD_RULES: ../../../../external/rules/projects/byul/build-rules.org
#+TEST_RULES: ../../../../external/rules/projects/byul/test-rules.org
#+TODO_PARENT: todo-리팩토링-navsys-headers.org
#+CONSUMER_INVENTORY: dstar-lite-key-cpp-consumer-inventory.json
#+AUTHOR: ByulPapa / Codex
#+LANGUAGE: ko
#+OPTIONS: toc:t num:nil

* Build compile-definition 불변식

- module static consumer는 =BYUL_STATIC=으로 C key ABI와 private comparator를 컴파일한다.
- root shared source는 같은 helper를 =BYUL_EXPORTS= 경로에서 재컴파일한다. helper 자체는
  export/install하지 않되 두 compile path의 exact ordering이 같음을 검사한다.

* 문서 책임과 결론

=byul/navsys/dstar_lite/dstar_lite_key.hpp=의 C++ helper를 전수 조사한다. 공통 기준은
[[file:todo-navsys-industry-api-baseline.org][Navsys 업계 API 기준]]이다. 이 파일은 현재 D* Lite public header install 목록에 들어가지만
실제 production consumer는 =dstar_lite_pqueue.cpp=뿐이다. 전 선언을 internalize하고
설치 SDK에서 제거한다.

* 실행 소유권과 테스트 위치

- 이 문서는 C key API를 소비하는 최소 C++ comparator/hash helper, namespace/ODR와
  public install에서 private detail로 옮기는 source-compatibility 경계만 소유한다.
- Key 수치 domain과 exact relation은 =dstar_lite_key.h= 문서, queue storage/container
  선택과 operation은 =dstar_lite_pqueue.h= 문서가 소유한다. 이 문서는 pqueue storage를
  직접 전환하지 않고 private helper와 소비 계약만 제공한다.
- 현재 =.hpp= 전용 native test 파일은 없다. Comparator property, two-TU ODR와 실제
  pqueue 소비 assertion은 shared test
  =byul/navsys/dstar_lite/tests/test_module_dstar_lite_pqueue.cpp=에 추가한다.
- Installed SDK exclusion/source-compatibility 검증은 해당 test target의 install fixture와
  root =byul/navsys/tests/test_module_navsys.cpp=에서 합류한다.

* 현재 구조 결함

- project license, =@file/@brief=, BYUL guard와 namespace가 없다.
- 사용하지 않는 =coord.h=, =vector=, =map= include가 있다.
- approximate =DstarLiteKeyEqual=, exact-bit hash, exact pointer less와
  approximate-first C compare가 한 파일에 공존한다.
- pointer functor가 NULL을 dereference하고 key lifetime을 container 밖에 의존한다.
- =std::hash<dstar_lite_key_t>=가 public mutable struct에 process-wide specialization을
  추가한다.

* 현행 선언 전수 목록과 처분

| 선언 | 현재 의미/결함 | 처분 |
|------+----------------+------|
| =DstarLiteKeyHash= | pointer exact hash | ABI 1.x: keep; conformance-block -> ABI-major: internalize |
| =DstarLiteKeyEqual= | pointer approximate equality | ABI 1.x: keep; conformance-block -> ABI-major: internalize |
| =DstarLiteKeyLess= | pointer exact lexicographic less | ABI 1.x: deprecate-forward -> ABI-major: internalize |
| =std::hash<dstar_lite_key_t>= | global mutable-key specialization | ABI 1.x: keep; conformance-block -> ABI-major: internalize |
| =dstar_lite_key_ptr_less= | C compare 기반 pointer less | ABI 1.x: deprecate-forward -> ABI-major: internalize |

이 파일에는 위 다섯 선언 외 public macro/type/function이 없다. 이름을 유지해 public
source contract로 승격하지 않는다.

* 업계 기준과 목표 detail header

- ordered comparator는
  [[https://eel.is/c++draft/associative.reqmts][C++ associative requirements]]의 strict weak
  ordering을 충족해야 한다. approximate equality는 사용할 수 없다.
- queue entry는 key와 coord를 값으로 소유한다. pointer comparator와 heap key allocation을
  함께 제거한다.
- 한 exact canonical C compare를 C/C++ 양쪽이 공유하고 detail functor는 이를 호출한다.

#+begin_src cpp
#ifndef BYUL_NAVSYS_DSTAR_LITE_INTERNAL_KEY_ORDER_HPP
#define BYUL_NAVSYS_DSTAR_LITE_INTERNAL_KEY_ORDER_HPP

namespace byul::navsys::dstar_lite_detail {

struct key_less final {
    [[nodiscard]] bool operator()(
        const dstar_lite_key_t& lhs,
        const dstar_lite_key_t& rhs) const noexcept {
        return dstar_lite_key_compare_exact(&lhs, &rhs) < 0;
    }
};

struct key_equal final {
    [[nodiscard]] bool operator()(
        const dstar_lite_key_t& lhs,
        const dstar_lite_key_t& rhs) const noexcept;
};

struct key_hash final {
    [[nodiscard]] std::size_t operator()(
        const dstar_lite_key_t& key) const noexcept;
};

} // namespace byul::navsys::dstar_lite_detail
#endif
#+end_src

- less/equal/hash는 동일한 canonical ±0/Inf/NaN 정책을 공유한다.
- 실제 pqueue가 ordered container만 사용하면 =key_equal/hash=도 만들지 않는다.
  speculative helper를 보존하지 않는다.
- C++ public convenience layer가 필요하면 별도 versioned header로 source compatibility,
  namespace와 exception policy를 새로 심사한다.

* CMake와 compatibility 계획

- =dstar_lite/CMakeLists.txt=의 public =dstar_lite_HEADERS=에서
  =dstar_lite_key.hpp=를 제거한다.
- internal header는 source list 또는 private file set에만 두고 install/export/wrapper에서
  제외한다. static module과 root shared는 같은 private file을 컴파일한다.
- repository 검색상 production consumer는 pqueue 하나지만 clean installed SDK를 쓰는
  external source fixture와 package history도 확인한다.
- external consumer가 있으면 기존 path를 한 major 동안 deprecated forwarding header로
  유지할 수 있다. 다만 broken approximate hash/equality를 계속 제공하는 대신 migration
  compile error 또는 명시적 legacy namespace를 선택한다.
- ABI 1은 inline C++ helper라 binary export보다 source compatibility가 문제다. 현행 설치
  SDK를 사용한 source fixture를 보존하고, 확인된 소비자에는 deprecated forwarding path와
  exact comparator migration을 제공한다.
- ABI 2에서는 이 header와 다섯 global 선언을 public SDK에서 종료한다. module static과
  root shared에 필요한 private detail만 남기고 wrapper/installed manifest에 exclusion
  reason을 고정한다.

* 검증 항목

- exact comparator: irreflexive, asymmetric, transitive, equivalence transitive.
- exact equality/hash: equal ⇒ same hash, ±0 canonical, 허용된 +Inf, NaN rejection.
- include: C++17 standalone, 최소 include, include order, two-TU ODR.
- inventory: public install에 없음, root export/C wrapper에 없음, static/root source는 동일.
- performance: by-value key의 comparison/allocation 수를 현행 heap-pointer map과 비교한다.

* 구현 순서

** DONE 1단계: consumer와 install 사실 확정

*** 실행

- 다섯 선언을 repository, installed tree, external fixture에서 전수 검색한다.
- public CMake header inventory와 wrapper manifest를 비교한다.

*** 완료 조건

- 각 consumer가 internal/test/external로 분류되고 forwarding 필요 여부가 결정된다.
- 이 파일이 public contract가 아닌 근거가 manifest에 기록된다.

*** 검증

- clean install file list와 five-symbol source report를 자동 생성한다.
- old SDK source fixture가 실제로 존재하면 별도 build한다.

*** 완료 기록

- 2026-07-25 =tools/dstar_lite_key_cpp_inventory.py=와
  =dstar-lite-key-cpp-consumer-inventory.json=으로 repository, external source, CMake,
  clean install과 header-role manifest의 판정을 하나의 재생성 가능한 report로 고정했다.
- 다섯 선언 중 production에서 소비되는 것은
  =dstar_lite_pqueue.cpp=의 =DstarLiteKeyLess= 하나뿐이다. =DstarLiteKeyHash=,
  =DstarLiteKeyEqual=, =std::hash<dstar_lite_key_t>=와
  =dstar_lite_key_ptr_less=는 선언 외 consumer가 없고 test-only/external consumer도 0개다.
- MSVC Release stage-4 clean install의 =include/byul/dstar_lite_key.hpp=는 source와 같은
  SHA-256이며 CMake ABI 1.x public header inventory에 포함된다. C++ helper export는 없고
  wrapper manifest는 독립 C surface가 없다는 이유로 =excluded=다.
- 별도 old-SDK external source fixture는 발견되지 않아 fixture build는 적용 대상이 아니다.
  그러나 이 경로가 기존 SDK에 실제 배포됐으므로 forwarding은 생략하지 않는다. 승인된
  header-role manifest대로 1.1에서 deprecated compatibility include로 유지하고 2.0에서
  비설치 =internal/dstar_lite_key_ops.hpp=로 종료한다.

** DONE 2단계: exact by-value detail comparator 도입

*** 실행

- license/file docs/BYUL guard/namespace를 갖춘 private header를 만든다.
- 필요한 최소 functor만 exact canonical C key API 위에 구현한다.

*** 완료 조건

- production comparator가 pointer, NULL, approximate equality를 사용하지 않는다.
- unused include와 global namespace 선언이 없다.

*** 검증

- ordering/hash property test와 C++17 two-TU ODR test를 실행한다.
- clang/gcc/msvc warning-as-error include audit를 실행한다.

*** 완료 기록

- 2026-07-25 =internal/dstar_lite_key_ops.hpp=에 license, =@file/@brief=,
  BYUL guard와 =byul::navsys::dstar_lite_detail= namespace를 갖춘 =key_less=만
  도입했다. ordered container가 요구하지 않는 =key_equal/key_hash=는 만들지 않았다.
- =key_less=는 두 canonical C key 값을 const reference로 받고 =noexcept=이며
  =dstar_lite_key_compare_exact=만 호출한다. 새 helper surface에는 pointer, NULL,
  approximate equality와 global namespace 선언이 없다.
- MSVC Release module/root build에서 two-TU ODR source를 =/W4 /WX=로 컴파일했다.
  exact relation 집중 test는 module/root 각각 1 case, 1410 assertions를 통과했고,
  D* Lite module 전체는 32 cases, 1581 assertions를 통과했다.
- GitHub Actions run =30156835676=은 2분에 성공했다. Ubuntu에서 GCC/Clang C++17
  =-Wall -Wextra -Werror= two-TU compile, private-helper inventory, clean SDK consumer,
  static/root UBSan gate와 139-header ABI role audit를 모두 통과했다.
- C++ helper inventory는 helpers=5, production consumers=1, external=0이며 Navsys ABI
  inventory는 headers=46, symbols=525다. inventory SHA는 CRLF/LF에 무관하도록
  정규화해 Windows와 Linux checkout에서 동일하게 검사한다.
- 현행 =dstar_lite_pqueue.cpp=의 legacy pointer comparator/storage는 이 단계에서
  전환하지 않았다. 새 canonical production surface만 exact by-value로 동결했고 실제
  pqueue 소비 전환은 이 문서 3단계와 pqueue 문서 TODO 2단계의 소유 경계를 따른다.

** DONE 3단계: private helper와 build inventory 전환

*** 실행

- pqueue owner가 소비할 exact by-value detail helper surface를 동결한다. 실제 pqueue
  storage/comparator 전환은 pqueue 문서의 TODO 2단계가 수행한다.
- CMake public header 목록에서 현행 =.hpp=를 제거하고 private path를 연결한다.

*** 완료 조건

- 현행 다섯 이름을 production source가 참조하지 않는다.
- internal header가 설치, export 또는 Python ABI manifest에 나타나지 않는다.

*** 검증

- static D* Lite/root shared build와 clean install inventory를 실행한다.
- forbidden symbol/path scan을 CI gate로 실행한다.

*** 완료 기록

- 2026-07-25 =dstar_lite_key.hpp=를 =dstar_lite_HEADERS=에서 제거하고
  =internal/dstar_lite_key_ops.hpp=를 =dstar_lite_SOURCES=에 연결했다. 생성된 MSVC
  project에서 private helper가 =dstar_lite= static, =navsys= static과 root =byul= shared
  세 target에 모두 포함되고 legacy header는 포함되지 않음을 확인했다.
- =dstar_lite_pqueue.cpp=는 현행 pointer-key storage를 유지하는 local transitional adapter
  안에서 by-value =dstar_lite_detail::key_less=를 호출한다. 따라서 이 문서가 소유하지 않는
  queue storage 전환 없이도 다섯 legacy helper 이름의 production 참조를 0으로 만들었다.
- 새 MSVC Release clean install은 headers=92이며 legacy =dstar_lite_key.hpp=와 private
  =dstar_lite_key_ops.hpp=를 모두 설치하지 않는다. C17/C++17 SDK consumer를 이 install로
  새로 configure/build/run했다.
- MSVC Release에서 module static, root shared, =test_dstar_lite=와 =test_byul=을
  재빌드했다. exact relation 집중 test는 module/root 각각 1 case, 1410 assertions,
  D* Lite module 전체는 32 cases, 1581 assertions를 통과했다.
- header-role manifest는 legacy path의 =approved_install=false=와 private helper의
  internal/wrapper-excluded 경계를 기록한다. Navsys ABI inventory는 private header를
  symbols=0으로 포함해 headers=47, symbols=525이며 CFFI 생성 결과는 변하지 않았다.
- GitHub Actions run =30157358793=은 2분 24초에 성공했다. Linux clean install은
  install-files=93, headers=92였고 forbidden inventory는 helpers=5, consumers=0,
  external=0이었다. GCC/Clang C++17 =-Werror=, header ABI compile=190/190,
  clean SDK consumer와 static/root UBSan gate도 모두 통과했다.
- Legacy source file과 그 안의 다섯 선언은 Stage 4의 compatibility-path 종료 전까지
  repository에만 남는다. 실제 pqueue by-value storage 전환은 pqueue 문서 TODO 2단계가
  계속 소유한다.

** DONE 4단계: compatibility path 종료

*** 실행

- 확인된 external consumer에 migration note/필요한 forwarding 기간을 제공한다.
- major version 종료 조건에 맞춰 기존 header를 제거한다.

*** 완료 조건

- filesystem/CMake/문서가 모두 private helper 역할에 일치한다.
- approximate comparator/hash 조합이 어떤 compatibility path에도 남지 않는다.

*** 검증

- old fixture(필요 시), 새 pqueue, installed SDK consumer와 =git diff --check=를 통과한다.

*** 완료 기록

- 2026-07-25 external source consumer와 legacy include consumer가 모두 0임을 다시 확인했다.
  따라서 migration fixture나 추가 forwarding 기간은 적용 대상이 아니며, major-version
  종료 gate에 따라 repository의 =dstar_lite_key.hpp=와 그 안의 다섯 C++ helper 선언을
  삭제했다.
- header-role manifest에서 legacy compatibility-forwarder row를 제거했다. 현재 canonical
  =internal/dstar_lite_key_ops.hpp=만 internal, =approved_install=false=,
  wrapper-excluded로 남는다. CMake public 목록에는 legacy path가 없고 private source
  목록에는 canonical helper가 있다.
- Stage 4 inventory schema는 repository path, manifest role, include, production/test/external
  symbol reference와 clean install을 모두 forbidden surface로 검사한다. 결과는
  legacy-helpers=0, consumers=0, external=0이며 approximate comparator/hash 조합과
  =std::hash<dstar_lite_key_t>= specialization은 어떤 compatibility path에도 남지 않는다.
- 새 MSVC Debug clean install은 headers=92이고 legacy =dstar_lite_key.hpp=와 private
  =dstar_lite_key_ops.hpp=를 모두 설치하지 않는다. 이 install만 사용하는 독립 C17/C++17
  SDK consumer를 새 build tree에서 configure/build/run했다.
- MSVC Debug에서 =dstar_lite= static, =navsys= static과 root =byul= shared를 재빌드했다.
  D* Lite module 전체는 32 cases, 1581 assertions, navsys module은 22 cases,
  142 assertions를 통과했다. header/ABI audit은 headers=138, public-c=95,
  declarations=1040, standalone compile=190/190, errors=0이었다.
- Navsys ABI inventory는 제거된 symbol-free compatibility header만 제외해 headers=46,
  symbols=525가 되었다. 설치된 C header와 C ABI symbol은 바뀌지 않았고, source-level
  legacy C++ include 제거만 developer 대상 major/breaking release-note 후보로 기록했다.
- 구현 commit =b875ae5=의 GitHub Actions run =30157882284=은 2분 10초에 성공했다.
  GCC/Clang C++17 =-Werror= private-helper compile, forbidden inventory, live header/export/
  wrapper audit, clean C17/C++17 SDK consumer와 static/root UBSan gate를 모두 통과했다.
- old fixture는 확인된 external consumer가 없어 실행 대상이 아니었다. 마지막으로
  =git diff --check=를 통과했고 실제 pqueue by-value storage 전환은
  =todo-navsys-dstar-lite-dstar-lite-pqueue.org= TODO 2단계의 소유권으로 남는다.
